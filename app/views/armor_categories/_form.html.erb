<%= form_with(model: armor_category) do |form| %>
  <% if armor_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(armor_category.errors.count, "error") %> prohibited this armor_category from being saved:</h2>

      <ul>
        <% armor_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :categoryId %>
    <%= form.text_field :categoryId %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
