<%= form_with(model: armor_order) do |form| %>
  <% if armor_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(armor_order.errors.count, "error") %> prohibited this armor_order from being saved:</h2>

      <ul>
        <% armor_order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.number_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :armor_id %>
    <%= form.number_field :armor_id %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
